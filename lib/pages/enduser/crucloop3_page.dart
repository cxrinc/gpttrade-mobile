import 'package:flutter/material.dart';

class Crucloop3Page extends StatelessWidget {
  const Crucloop3Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Center(child: Text('CruCLoop3 Page')));
  }
}
