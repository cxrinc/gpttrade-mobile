import 'package:flutter/material.dart';

class Crucloop2Page extends StatelessWidget {
  const Crucloop2Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Center(child: Text('CruCLoop2 Page')));
  }
}
